# Impronta delle migrazioni già distribuite.
#
# Una migrazione applicata non si modifica mai: chi la cambia rompe ogni
# database che l'ha già eseguita, che al riavvio si rifiuta di aprirsi.
# Per correggere una migrazione se ne aggiunge una nuova.
#
# La prova `migrations_are_frozen` confronta questo file con i file veri.
# Aggiungendo una migrazione si aggiunge qui la sua riga; per tutto il
# resto, se la prova fallisce il rimedio non è aggiornare questo file.

# Consolidamento pre-1.0 (14 settembre 2026): 0002-0005 fuse nella baseline
# prima di unirle a main, che non le aveva mai ricevute. Lecito solo finché
# nessun database distribuito le ha applicate: chi le aveva in locale ricrea
# il database e reimporta un backup. Dopo la 1.0 questa strada è chiusa.
#
# Consolidamento pre-1.0 (21 settembre 2026): 0002_excluded_pages e
# 0003_single_pdf_copy fuse nella baseline prima di unire la PR #475 a main,
# che non le aveva mai ricevute. Stessa condizione: nessun database
# distribuito le ha applicate.

# Consolidamento pre-1.0 (22 settembre 2026): colonne OCR/HTR (#220) su
# workspaces, transcription_documents, transcription_segments e
# operation_logs fuse nella baseline prima di unire il lavoro a main.
# Stessa condizione: nessun database distribuito le ha applicate.

# Consolidamento pre-1.0 (23 settembre 2026): eliminate due colonne OCR mai
# usate prima del merge. Database locale ricreato dopo export dei dati.

0001_baseline_2_0.sql 740cbb58eadb6d58
