# Sessione #296 — 2026-06-30 (parziale)

Branch: `feat/296-rail-redesign`
Stato: commit fatto, `tsc` + build Vite puliti. **Non testato in `tauri dev`.**

---

## Fatto

**Store**
- `configStore.ts`: `WorkMode = 'chunk' | 'all'` + `workMode`/`setWorkMode` (default `'chunk'`)
- `uiStore.ts`: `ChunkRailTab = 'audit' | 'notes' | 'memory'`, campi `chunkRailTab`, `showInsightPanel`, `showConsoleDrawer` + setter. Legacy `chunkDrawerTab`/`showChunkDrawer` mantenuti, retrocompat dashboard.

**Shell-next**
- `ShellNext.tsx`: `inspectorOpen` usa `showInsightPanel` (era `showDocumentDrawer || showChunkDrawer`)
- `ProjectInspectorNext.tsx`: riscritto — solo `InsightDocPanel`, no tab Frammento
- `ProjectRailNext.tsx`: redesign completo
  - Top `h-20`: [ChevronLeft collapse] + [LibraryBig]
  - Contenuto scrollabile: PipelineSidebarPipelinesSection + PipelineSidebarRunSection + ChunkInspectorPanel embedded
  - Bottom `h-12` fisso: [← Workspace] [⚙ Impostazioni] [↑ Importa] [↓ Esporta]
  - Collassata: [ChevronRight expand] + azione primaria workMode + stesse 4 icone in colonna
  - Props rimosse: `onDryRun`, `onRunAuditOnly`. Aggiunta: `onReauditChunk`.

**InsightsDrawer**
- `ChunkInspectorPanel`: 3 tab (Audit, Note, Memoria). Rimossi Summary + Operations. Rimossa prop `onClose` + `FlyoutHeader`. Usa `chunkRailTab`/`setChunkRailTab`.

**PipelineSidebarSections**
- `PipelineSidebarRunSection`: aggiunto ramo `playFirst`:
  - Switch Chunk/Tutto (pulsanti toggle, rounded-full border)
  - `workMode === 'chunk'`: label § N + pulsante "↺ Traduci chunk"
  - `workMode === 'all'`: pulsante "▶ Esegui tutto" / "◼ Stop" + progresso X/Y
  - Sandbox/PipelineConfig view invariata (non-`playFirst`)

**AppStatusBar**
- Centro: `ChunkCenterStats` — `§ N · X w · [icona qualità] · stato`
  - Hover "X w": tooltip "N parole"
  - Hover icona qualità: tooltip "Qualità ottima" (usa `qualityLabelKey` reale, non tono generico)
- Sinistra: solo pannello attivo (rimosso breadcrumb progetto/pipeline)
- Destra: console toggle (Terminal) + separatore + controlli vista + salvataggio
- `ConsoleDrawer`: `OperationsTab` embedded, `absolute bottom-full`, altezza `h-64`, togglabile da `showConsoleDrawer`

**Callers migrati**
- `AuditPanel.tsx`, `DocumentView.tsx`, `IssueList.tsx`: `setShowChunkDrawer(true, 'notes')` → `setChunkRailTab('notes')`
- `chunksStore.ts`: rimossa `ui.setShowChunkDrawer(true)` al caricamento documento
- `App.tsx`: rimossi `onDryRun`/`onRunAuditOnly` da `<ShellNext>`

**i18n** (it.json + en.json)
- `sidebar.collapse`, `sidebar.expand`
- `pipeline.workModeChunk`, `pipeline.workModeAll`, `pipeline.translateChunk`, `pipeline.executeAll`
- `statusBar.quality.{strong,ok,weak,tooltipPrefix}`, `statusBar.chunkProcessing`, `statusBar.chunkWords`, `statusBar.chunkWordsTooltip`
- `console.toggle`, `console.title`

---

## Fatto (sessione 2 — stesso giorno)

### Pipeline section — active-only + popover "Cambia" ✅
`PipelineSidebarPipelinesSection` con `configTrigger='circle'`: card compatto nome pipeline attiva + [⚙ Configura] + [⇄ Cambia] (Radix Popover). Popover lista tutte pipeline + [+ Nuova pipeline].

### useKeyboardShortcuts ✅
Rimosso `onDryRun`. `Ctrl+Enter` legge `workMode`: chunk → `handleRetranslateChunk(selectedChunkId)`, all → `runPipeline()`.

### ConfigDrawer.handleResetAll ✅
Aggiunto `setWorkMode('chunk')` al reset.

### Test ✅ (542 verdi, 0 fallimenti)
- `AppStatusBar.test.tsx`: aggiornato — progetto non mostra più nome, verifica console toggle
- `PipelineSidebarSections.test.tsx`: aggiornato per card-based UI + popover Cambia
- `ShellNext.test.tsx`: rimosso assert `tablist` (ChunkInspectorPanel ha propri tab nella rail)

### i18n ✅
`pipeline.changePipeline` aggiunto (it/en).

---

## Fatto (sessione 3 — 2026-07-01)

### Polish rail sinistra ✅
- Animazione collapse/expand riallineata ai token motion condivisi shell (`duration-300`, curva editoriale, no transition durante drag); cambio contenuto aperto/chiuso ritardato in collasso, riduce scatto percepito.
- Versione collassata: rimosso separatore superiore fantasma; top ora coerente con barra aperta.
- Controlli frammento precedente/successivo spostati da testata centrale a rail sinistra: orizzontali in barra aperta, verticali in barra collassata; dimensione aumentata per allinearli al peso tab.
- Rimosso ovale attorno controlli frammento in testata rail.
- Testata centrale documento alleggerita: restano stati pipeline + minimap frammenti, non più frecce + contatore.
- Area Audit/Note/Memoria: aggiunto separatore superiore leggero, chiarisce pannello frammento sotto comandi esecuzione.
- Larghezza minima rail aperta aumentata, non rompe contenuti tab Note; default riallineato, larghezze salvate clamped al nuovo intervallo.
- Switch Chunk/Tutto + azione primaria integrati in testata pipeline: controlli icon-only con tooltip, no card/slider dedicati; "Traduci chunk" senza testo visibile.
- Audit UX barra sinistra: gerarchia confermata 4 livelli — navigazione frammento in alto, identità/comandi pipeline sotto, tab Audit/Note/Memoria come soglia pannello frammento, contenuto tab in basso.
- Nome pipeline più compatto, avvicinato ai comandi; rimossa spaziatura verticale grande tra testata e pipeline.
- Modalità Chunk/Tutto mantenuta in blocco pipeline, sopra tab e accanto azione primaria: non spostata in riga alta, lì appartengono collasso + navigazione frammento.
- Scroll confinato a contenuto tab Audit/Note/Memoria: con molti problemi/commenti resta fermo testata barra, resta ferma tabbar, scorre solo contenuto in basso.
- Tab Audit/Note: azioni limitate a riga titolo, testi principali a tutta larghezza; righe editoriali con separatori orizzontali sottili.
- Audit: linea verticale tipo quote reintrodotta solo in dettagli annidati (traduzione/frase sorgente/correzione), non su riga principale.
- Scelta UX confermata: tab frammento restano sotto comandi pipeline; no tab "Pipeline" aggiunto, confonderebbe gerarchia e nasconderebbe azione principale.

### Accenti / token CSS ✅
- `editorial-accent` spostato da rosso a verde petrolio.
- Aggiunto `editorial-danger` per stop, conferme distruttive, errori bloccanti.
- Stop pipeline, conferme distruttive, errori, qualità debole, annotazioni problematiche spostati su `editorial-danger`, evita diventino verdi.
- Marker note sorgente/traduzione rossi; marker annotazioni non più a larghezza zero, evita sovrapposizioni su testo.
- Tab Note alleggerito: rimosse card rettangolari per singola nota, sostituite righe editoriali con separatori orizzontali; note sorgente in rosso.
- Puliti token laterali non definiti (`editorial-line`, `editorial-surface`, `editorial-hover`), sostituiti da token esistenti.
- `docs-dev/UI_DESIGN_SYSTEM.md` aggiornato con nuovi token + regole accent/danger.

### Verifiche ✅
- `npm run lint` pulito (`tsc --noEmit`)
- Test mirati puliti: `PipelineSidebarSections.test.tsx`, `ShellNext.test.tsx`, `AppStatusBar.test.tsx` (11 test)
- Suite completa pulita: 543 test verdi
- `npm run build` pulito; resta solo warning Vite su chunk grandi.
- GitHub issue #296 aggiornata con commento avanzamento; milestone verificata: `v1.1 features`.

---

## Da fare (prossima sessione)

### 2. Verifica UI in tauri dev [priorità alta — prima di tutto il resto]
```
pnpm tauri dev
```
Aprire progetto con documento (≥3 chunk). Checklist:
- [ ] Collapse/expand rail (ChevronLeft/Right funzionanti)
- [ ] Controlli frammento precedente/successivo in rail sinistra funzionanti anche da collassata
- [ ] Pulsanti icon-only Chunk/Tutto visibili, cliccabili, chiari via tooltip
- [ ] Pulsante "Traduci chunk" icon-only traduce chunk selezionato, mostra tooltip
- [ ] Pulsante "Esegui tutto" icon-only avvia pipeline completa, mostra tooltip
- [ ] Pulsante "Stop" icon-only ferma esecuzione, mostra tooltip
- [ ] Console toggle apre/chiude drawer sopra status bar
- [ ] ChunkInspectorPanel 3 tab (Audit/Note/Memoria) nella rail
- [ ] Audit/Note/Memoria con molti contenuti: scorre solo contenuto tab, non tutta barra
- [ ] Pannello destra solo Insight (no tab Frammento)
- [ ] Status bar centro: § N · X w · icona qualità · stato
- [ ] Bottom rail 4 azioni funzionanti

### 3. Rifiniture visive da valutare dopo verifica
- ChunkInspectorPanel in rail: altezza/overflow OK? Nuovo separatore vs RunSection funziona a video?
- Controlli frammento in testata rail: posizione/peso visivo OK?
- Pulsanti Chunk/Tutto icon-only: icone + stato attivo approvati o da rivedere?
- Tab Audit: righe problemi leggibili senza card? Azioni in hover abbastanza scopribili?
- Bottom rail h-12: 4 icone `justify-around` abbastanza spaziose?
- Collassata: icone in colonna leggibili (padding verticale sufficiente)? Valutare nascondere import disabilitato.
- ConsoleDrawer: h-64 altezza giusta o troppo grande/piccola?

### 4. useKeyboardShortcuts — workMode ✅ (fatto sessione 2)

### 5. ConfigDrawer — reset ✅ (fatto sessione 2)

### 6. Test ✅ (fatto sessione 2)

### 7. Docs
Aggiornare `docs-dev/ARCHITECTURE.md`:
- Nuovi campi uiStore (`showInsightPanel`, `chunkRailTab`, `showConsoleDrawer`)
- Nuovo campo configStore (`workMode`)
- Nuovo layout rail (collapse/expand, bottom fisso, ChunkInspectorPanel embedded)
- Console drawer (OperationsTab → AppStatusBar)
- Status bar chunk-stats

### 8. Issue separata — persistenza Riepilogo
`operationLogStore` in-memory: dati Operations/Summary spariscono al riavvio.
Non parte di #296. Creare issue GitHub separata, etichetta "bug / ux".

---

## File toccati in questa sessione

```
src/App.tsx
src/components/audit/AuditPanel.tsx
src/components/common/StatusIndicator.tsx
src/components/document/DocumentView.tsx
src/components/document/InsightsDrawer.tsx
src/components/document/StageTraceDialog.tsx
src/components/document/tabs/AuditTab.tsx
src/components/document/tabs/CoherenceTab.tsx
src/components/document/tabs/IndexTab.tsx
src/components/document/tabs/IssueList.tsx
src/components/document/tabs/NotesTab.tsx
src/components/document/tabs/StatsTab.tsx
src/components/layout/AppStatusBar.tsx
src/components/layout/PipelineSidebarSections.tsx
src/components/layout/motion.ts
src/components/layout/shell-next/ProjectInspectorNext.tsx
src/components/layout/shell-next/ProjectRailNext.tsx
src/components/layout/shell-next/ShellNext.tsx
src/components/layout/shell-next/ShellNext.test.tsx
src/components/ui/AlertDialog.tsx
src/components/ui/IconButton.tsx
src/components/ui/StatusDot.tsx
src/index.css
docs-dev/UI_DESIGN_SYSTEM.md
src/i18n/en.json
src/i18n/it.json
src/stores/chunksStore.ts
src/stores/configStore.ts
src/stores/uiStore.ts
```