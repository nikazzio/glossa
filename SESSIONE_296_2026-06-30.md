# Sessione #296 — 2026-06-30 (parziale)

Branch: `feat/296-rail-redesign`  
Stato: commit fatto, `tsc` e build Vite puliti. **Non ancora testato in `tauri dev`.**

---

## Fatto

**Store**
- `configStore.ts`: `WorkMode = 'chunk' | 'all'` + `workMode`/`setWorkMode` (default `'chunk'`)
- `uiStore.ts`: `ChunkRailTab = 'audit' | 'notes' | 'memory'`, campi `chunkRailTab`, `showInsightPanel`, `showConsoleDrawer` + setter. Legacy `chunkDrawerTab`/`showChunkDrawer` mantenuti per retrocompatibilità dashboard.

**Shell-next**
- `ShellNext.tsx`: `inspectorOpen` usa `showInsightPanel` (era `showDocumentDrawer || showChunkDrawer`)
- `ProjectInspectorNext.tsx`: riscritto — solo `InsightDocPanel`, nessuna tab Frammento
- `ProjectRailNext.tsx`: redesign completo
  - Top `h-20`: [ChevronLeft collapse] + [LibraryBig]
  - Contenuto scrollabile: PipelineSidebarPipelinesSection + PipelineSidebarRunSection + ChunkInspectorPanel embedded
  - Bottom `h-12` fisso: [← Workspace] [⚙ Impostazioni] [↑ Importa] [↓ Esporta]
  - Collassata: [ChevronRight expand] + azione primaria workMode + stesse 4 icone in colonna
  - Props rimosse: `onDryRun`, `onRunAuditOnly`. Aggiunta: `onReauditChunk`.

**InsightsDrawer**
- `ChunkInspectorPanel`: 3 tab (Audit, Note, Memoria). Rimossi Summary e Operations. Rimossa prop `onClose` e `FlyoutHeader`. Usa `chunkRailTab`/`setChunkRailTab`.

**PipelineSidebarSections**
- `PipelineSidebarRunSection`: aggiunto ramo `playFirst`:
  - Switch Chunk/Tutto (pulsanti toggle, rounded-full border)
  - `workMode === 'chunk'`: label § N + pulsante "↺ Traduci chunk"
  - `workMode === 'all'`: pulsante "▶ Esegui tutto" / "◼ Stop" + progresso X/Y
  - Sandbox/PipelineConfig view invariata (non-`playFirst`)

**AppStatusBar**
- Centro: `ChunkCenterStats` — `§ N · X w · [icona qualità] · stato`
  - Hover su "X w": tooltip "N parole"
  - Hover su icona qualità: tooltip "Qualità ottima" (usa `qualityLabelKey` reale, non tono generico)
- Sinistra: solo pannello attivo (rimosso breadcrumb progetto/pipeline)
- Destra: console toggle (Terminal) + separatore + controlli vista + salvataggio
- `ConsoleDrawer`: `OperationsTab` embedded, `absolute bottom-full`, altezza `h-64`, togglabile da `showConsoleDrawer`

**Callers migrati**
- `AuditPanel.tsx`, `DocumentView.tsx`, `IssueList.tsx`: `setShowChunkDrawer(true, 'notes')` → `setChunkRailTab('notes')`
- `chunksStore.ts`: rimossa `ui.setShowChunkDrawer(true)` al caricamento documento
- `App.tsx`: rimossi `onDryRun`/`onRunAuditOnly` da `<ShellNext>`

**i18n** (it.json + en.json)
- `sidebar.collapse`, `sidebar.expand`
- `pipeline.workModeChunk`, `pipeline.workModeAll`, `pipeline.translateChunk`, `pipeline.executeAll`
- `statusBar.quality.{strong,ok,weak,tooltipPrefix}`, `statusBar.chunkProcessing`, `statusBar.chunkWords`, `statusBar.chunkWordsTooltip`
- `console.toggle`, `console.title`

---

## Fatto (sessione 2 — stesso giorno)

### Pipeline section — active-only + popover "Cambia" ✅
`PipelineSidebarPipelinesSection` con `configTrigger='circle'`: card compatto nome pipeline attiva + [⚙ Configura] + [⇄ Cambia] (Radix Popover). Popover lista tutte le pipeline + [+ Nuova pipeline].

### useKeyboardShortcuts ✅
Rimosso `onDryRun`. `Ctrl+Enter` ora legge `workMode`: chunk → `handleRetranslateChunk(selectedChunkId)`, all → `runPipeline()`.

### ConfigDrawer.handleResetAll ✅
Aggiunto `setWorkMode('chunk')` al reset.

### Test ✅ (542 verdi, 0 fallimenti)
- `AppStatusBar.test.tsx`: aggiornato — progetto non mostra più nome, verifica console toggle
- `PipelineSidebarSections.test.tsx`: aggiornato per card-based UI + popover Cambia
- `ShellNext.test.tsx`: rimosso assert `tablist` (ChunkInspectorPanel ha propri tab nella rail)

### i18n ✅
`pipeline.changePipeline` aggiunto (it/en).

---

## Fatto (sessione 3 — 2026-07-01)

### Polish rail sinistra ✅
- Animazione collapse/expand riallineata ai token motion condivisi della shell (`duration-300`, curva editoriale, no transition durante drag); cambio contenuto aperto/chiuso ritardato in collasso per ridurre lo scatto percepito.
- Versione collassata: rimosso il separatore superiore fantasma; il top ora resta coerente con la barra aperta.
- Controlli frammento precedente/successivo spostati dalla testata centrale alla rail sinistra: orizzontali nella barra aperta, verticali nella barra collassata; dimensione aumentata per allinearli al peso dei tab.
- Rimosso l'ovale attorno ai controlli frammento nella testata rail.
- Testata centrale documento alleggerita: restano stati pipeline e minimap dei frammenti, non più frecce + contatore.
- Area Audit / Note / Memoria: aggiunto separatore superiore leggero per chiarire che è il pannello del frammento sotto i comandi di esecuzione.
- Larghezza minima rail aperta aumentata per non rompere i contenuti del tab Note; default riallineato e larghezze salvate clamped al nuovo intervallo.
- Switch Chunk/Tutto e azione primaria integrati nella testata della pipeline: controlli icon-only con tooltip, senza card/slider dedicati; "Traduci chunk" non mostra più testo visibile.
- Audit UX barra sinistra: gerarchia confermata in 4 livelli — navigazione frammento in alto, identità/comandi pipeline sotto, tab Audit/Note/Memoria come soglia del pannello frammento, contenuto tab in basso.
- Nome pipeline reso più compatto e avvicinato ai comandi; rimossa la grande spaziatura verticale che separava troppo testata e pipeline.
- Modalità Chunk/Tutto mantenuta nel blocco pipeline, sopra i tab e accanto all'azione primaria: non spostata nella riga altissima perché lì appartengono collasso e navigazione del frammento.
- Scroll confinato al contenuto dei tab Audit/Note/Memoria: con molti problemi/commenti resta ferma la testata della barra, resta ferma la tabbar, e scorre solo il contenuto in basso.
- Tab Audit e Note: azioni limitate alla riga titolo, testi principali a tutta larghezza; righe editoriali con separatori orizzontali sottili.
- Audit: linea verticale tipo quote reintrodotta solo nei dettagli annidati (nella traduzione / frase sorgente / correzione), non sulla riga principale.
- Scelta UX confermata: i tab del frammento restano sotto i comandi pipeline; non aggiunto un tab "Pipeline" perché confonderebbe gerarchia e nasconderebbe l'azione principale.

### Accenti / token CSS ✅
- `editorial-accent` spostato dal rosso al verde petrolio.
- Aggiunto `editorial-danger` per stop, conferme distruttive ed errori bloccanti.
- Stop pipeline, conferme distruttive, errori, qualità debole e annotazioni problematiche spostati su `editorial-danger`, evitando che diventino verdi.
- Marker note sorgente/traduzione rossi; marker annotazioni non più a larghezza zero per evitare sovrapposizioni sul testo.
- Tab Note alleggerito: rimosse card rettangolari per singola nota, sostituite da righe editoriali con separatori orizzontali; note sorgente in rosso.
- Puliti token laterali non definiti (`editorial-line`, `editorial-surface`, `editorial-hover`) in favore dei token esistenti.
- `docs-dev/UI_DESIGN_SYSTEM.md` aggiornato con i nuovi token e le regole accent/danger.

### Verifiche ✅
- `npm run lint` pulito (`tsc --noEmit`)
- Test mirati puliti: `PipelineSidebarSections.test.tsx`, `ShellNext.test.tsx`, `AppStatusBar.test.tsx` (11 test)
- Suite completa pulita: 543 test verdi
- `npm run build` pulito; resta solo il warning Vite sui chunk grandi.
- GitHub issue #296 aggiornata con commento di avanzamento; milestone verificata: `v1.1 features`.

---

## Da fare (prossima sessione)

### 2. Verifica UI in tauri dev [priorità alta — da fare prima di tutto il resto]
```
pnpm tauri dev
```
Aprire progetto con documento (≥3 chunk). Checklist:
- [ ] Collapse/expand rail (ChevronLeft/Right funzionanti)
- [ ] Controlli frammento precedente/successivo nella rail sinistra funzionanti anche da collassata
- [ ] Pulsanti icon-only Chunk/Tutto visibili, cliccabili e chiari via tooltip
- [ ] Pulsante "Traduci chunk" icon-only traduce il chunk selezionato e mostra tooltip
- [ ] Pulsante "Esegui tutto" icon-only avvia pipeline completa e mostra tooltip
- [ ] Pulsante "Stop" icon-only ferma esecuzione e mostra tooltip
- [ ] Console toggle apre/chiude drawer sopra status bar
- [ ] ChunkInspectorPanel 3 tab (Audit / Note / Memoria) nella rail
- [ ] Audit/Note/Memoria con molti contenuti: scorre solo il contenuto del tab, non tutta la barra
- [ ] Pannello destra solo Insight (nessuna tab Frammento)
- [ ] Status bar centro: § N · X w · icona qualità · stato
- [ ] Bottom rail 4 azioni funzionanti

### 3. Rifiniture visive da valutare dopo verifica
- ChunkInspectorPanel nella rail: altezza/overflow OK? Il nuovo separatore rispetto a RunSection funziona a video?
- Controlli frammento in testata rail: posizione e peso visivo OK?
- Pulsanti Chunk/Tutto icon-only: icone e stato attivo approvati o da rivedere?
- Tab Audit: righe problemi leggibili senza card? Azioni in hover sufficientemente scopribili?
- Bottom rail h-12: 4 icone `justify-around` abbastanza spaziose?
- Collassata: icone in colonna leggibili (padding verticale sufficiente)? Valutare se nascondere l'import disabilitato.
- ConsoleDrawer: h-64 è l'altezza giusta o troppo grande/piccola?

### 4. useKeyboardShortcuts — workMode ✅ (fatto sessione 2)

### 5. ConfigDrawer — reset ✅ (fatto sessione 2)

### 6. Test ✅ (fatto sessione 2)

### 7. Docs
Aggiornare `docs-dev/ARCHITECTURE.md`:
- Nuovi campi uiStore (`showInsightPanel`, `chunkRailTab`, `showConsoleDrawer`)
- Nuovo campo configStore (`workMode`)
- Nuovo layout rail (collapse/expand, bottom fisso, ChunkInspectorPanel embedded)
- Console drawer (OperationsTab → AppStatusBar)
- Status bar chunk-stats

### 8. Issue separata — persistenza Riepilogo
`operationLogStore` è in-memory: i dati Operations/Summary spariscono al riavvio.
Non parte di #296. Creare issue GitHub separata con etichetta "bug / ux".

---

## File toccati in questa sessione

```
src/App.tsx
src/components/audit/AuditPanel.tsx
src/components/common/StatusIndicator.tsx
src/components/document/DocumentView.tsx
src/components/document/InsightsDrawer.tsx
src/components/document/StageTraceDialog.tsx
src/components/document/tabs/AuditTab.tsx
src/components/document/tabs/CoherenceTab.tsx
src/components/document/tabs/IndexTab.tsx
src/components/document/tabs/IssueList.tsx
src/components/document/tabs/NotesTab.tsx
src/components/document/tabs/StatsTab.tsx
src/components/layout/AppStatusBar.tsx
src/components/layout/PipelineSidebarSections.tsx
src/components/layout/motion.ts
src/components/layout/shell-next/ProjectInspectorNext.tsx
src/components/layout/shell-next/ProjectRailNext.tsx
src/components/layout/shell-next/ShellNext.tsx
src/components/layout/shell-next/ShellNext.test.tsx
src/components/ui/AlertDialog.tsx
src/components/ui/IconButton.tsx
src/components/ui/StatusDot.tsx
src/index.css
docs-dev/UI_DESIGN_SYSTEM.md
src/i18n/en.json
src/i18n/it.json
src/stores/chunksStore.ts
src/stores/configStore.ts
src/stores/uiStore.ts
```
